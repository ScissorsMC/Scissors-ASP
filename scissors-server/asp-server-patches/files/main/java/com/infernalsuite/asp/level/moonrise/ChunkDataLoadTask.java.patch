--- a/main/java/com/infernalsuite/asp/level/moonrise/ChunkDataLoadTask.java
+++ b/main/java/com/infernalsuite/asp/level/moonrise/ChunkDataLoadTask.java
@@ -6,6 +_,7 @@
 import ca.spottedleaf.moonrise.patches.chunk_system.scheduling.task.ChunkLoadTask;
 import ca.spottedleaf.moonrise.patches.chunk_system.scheduling.task.GenericDataLoadTask;
 import com.infernalsuite.asp.api.world.SlimeChunk;
+import com.infernalsuite.asp.api.world.SlimeFlatWorldProfile;
 import com.mojang.logging.LogUtils;
 import net.minecraft.server.level.ServerLevel;
 import net.minecraft.world.level.ChunkPos;
@@ -64,9 +_,28 @@
 
     private ChunkAccess runOnMain(final SlimeChunk data) {
         try {
+            if (this.world.slimeInstance.isVanilla()) {
+                var tag = this.world.slimeInstance.vanillaData("chunk", chunkX, chunkZ);
+                if (tag == null) {
+                    return new net.minecraft.world.level.chunk.ProtoChunk(new ChunkPos(chunkX, chunkZ), UpgradeData.EMPTY,
+                            this.world, this.world.palettedContainerFactory(), null);
+                }
+                int version = tag.getIntOr("DataVersion", -1);
+                tag = ca.spottedleaf.moonrise.common.PlatformHooks.get().convertNBT(net.minecraft.util.datafix.fixes.References.CHUNK,
+                        this.world.getServer().getFixerUpper(), tag, version, net.minecraft.SharedConstants.getCurrentVersion().dataVersion().version());
+                var saved = net.minecraft.world.level.chunk.storage.SerializableChunkData.parse(this.world, this.world.palettedContainerFactory(), tag);
+                if (saved == null) {
+                    throw new IllegalStateException("Invalid saved vanilla chunk");
+                }
+                return saved.read(this.world, this.world.getPoiManager(), this.world.getChunkSource().chunkMap.storageInfo(), new ChunkPos(chunkX, chunkZ));
+            }
             LevelChunk chunk = this.world.slimeInstance.createChunk(chunkX, chunkZ, data);
             return new ImposterProtoChunk(chunk, false);
         } catch (final Exception e) {
+            if (this.world.slimeInstance.isVanilla() || this.world.slimeInstance.flatWorldGenerator() != null
+                    || this.world.slimeInstance.getExtraData().containsKey(SlimeFlatWorldProfile.EXTRA_DATA_KEY)) {
+                throw new IllegalStateException("Failed to load a generated flat-world chunk", e);
+            }
             LOGGER.error("Failed to parse main tasks for task {}, chunk data will be lost", this, e);
             return this.getEmptyChunk();
         }
