--- a/aspaper-server/build.gradle.kts
+++ b/aspaper-server/build.gradle.kts
@@ -23,6 +_,8 @@
     gitFilePatches = false
 
     val aspaper = forks.register("aspaper") {
+        rootDirectory = upstreamsDirectory().map { it.dir("aspaper") }
+
         upstream.patchDir("paperServer") {
             upstreamPath = "paper-server"
             excludes = setOf("src/minecraft", "patches", "build.gradle.kts")
@@ -31,7 +_,17 @@
         }
     }
 
-    activeFork = aspaper
+    val scissors = forks.register("scissors") {
+        forks = aspaper
+
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = aspaper.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("scissors-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+    }
+
+    activeFork = scissors
 
 //    updatingMinecraft {
 //        oldPaperCommit = "7e80cef5198561d0db53406127e5b8bc7af51577"
@@ -101,6 +_,9 @@
     main {
         java { srcDir("../paper-server/src/main/java") }
         resources { srcDir("../paper-server/src/main/resources") }
+        // Scissors-ASP - compile ASP's directly owned server sources from the generated asp-server worktree
+        java { srcDir("../asp-server/main/java") }
+        resources { srcDir("../asp-server/main/resources") }
     }
     test {
         java { srcDir("../paper-server/src/test/java") }
@@ -132,7 +_,7 @@
 }
 
 dependencies {
-    implementation(project(":aspaper-api")) //ASP
+    implementation(project(":scissors-api")) // Scissors-ASP
     implementation(project(":core")) //ASP
     implementation("ca.spottedleaf:leafpile:1.0.0")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
@@ -195,18 +_,19 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "AdvancedSlimePaper", //ASP
+            "Implementation-Title" to "Scissors", // Scissors-ASP
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "AdvancedSlimePaper", //ASP
+            "Specification-Title" to "Scissors", // Scissors-ASP
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "InfernalSuite Team", //ASP
-            "Brand-Id" to "infernalsuite:advancedslimepaper", //ASP
-            "Brand-Name" to "AdvancedSlimePaper", //ASP
+            "Specification-Vendor" to "ScissorsMC Team", // Scissors-ASP
+            "Brand-Id" to "scissorsmc:scissors", // Scissors-ASP
+            "Brand-Name" to "Scissors", // Scissors-ASP
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
             "Git-Commit" to gitHash,
+            "ASP-Ref" to rootProject.providers.gradleProperty("aspRef").get(), // Scissors-ASP
         )
         for (tld in setOf("net", "com", "org")) {
             attributes("$tld/bukkit", "Sealed" to true)
@@ -351,7 +_,7 @@
 }
 
 fill {
-    project("paper")
+    project("scissors-asp")
     versionFamily(paperweight.minecraftVersion.map { it.split(".", "-").takeWhile { part -> part.toIntOrNull() != null }.take(2).joinToString(".") })
     version(paperweight.minecraftVersion)
 
